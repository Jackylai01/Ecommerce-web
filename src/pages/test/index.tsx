import { Box } from '@chakra-ui/react';
import Footer from '@components/EditorPage/FrontPageUI/src/modernFooter';

const ProductGrid = () => {
  return (
    <Box>
      <Footer />
    </Box>
  );
};

export default ProductGrid;
